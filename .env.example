# Database (PostgreSQL) — e.g. local Postgres, Supabase, Neon, Railway
DATABASE_URL="postgresql://user:password@localhost:5432/events_partner?schema=public"

# Auth.js
AUTH_SECRET="replace-with-a-long-random-string"
NEXTAUTH_URL="http://localhost:3000"

# OAuth providers (optional — leave blank to hide the buttons)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# Email (Resend). Leave blank to run in MOCK EMAIL MODE (logged, not sent).
RESEND_API_KEY=""
EMAIL_FROM="Events Partner <no-reply@eventspartner.app>"

# SMS (Twilio-compatible). Leave blank to run in MOCK SMS MODE (logged, not sent).
SMS_PROVIDER_ACCOUNT_SID=""
SMS_PROVIDER_AUTH_TOKEN=""
SMS_PROVIDER_FROM_NUMBER=""

# Scheduled messages: Vercel Cron calls /api/cron/send-scheduled with "Authorization: Bearer <CRON_SECRET>".
# The endpoint refuses to run if this is unset. Generate with: openssl rand -base64 32
CRON_SECRET="replace-with-a-long-random-string"

# App — set to the public https:// URL in production (used for RSVP links, QR codes, emails, metadata)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Public contact address shown on the Privacy Policy and Terms pages (required before going live)
NEXT_PUBLIC_SUPPORT_EMAIL=

# Admin allowlist. ONLY these emails can be admins: an email here is promoted to ADMIN when it signs in with
# Google (on a local machine, registering with it also works), and any existing admin whose email is NOT listed
# here is demoted automatically. To remove someone's admin access, take their email out and redeploy.
ADMIN_BOOTSTRAP_EMAIL="chihenson13@gmail.com"

# Extra admin emails, comma-separated (same rules as above). Leave blank for a single admin.
ADMIN_EMAILS=

# Official Meta Messenger integration — keep DISABLED until the Meta app is approved (docs/meta-messenger-integration.md).
# Never commit real values. META_APP_SECRET and the tokens are only ever used server-side.
META_MESSENGER_ENABLED=false
META_APP_ID=
META_APP_SECRET=
# Must exactly match a "Valid OAuth Redirect URI" in the Meta app, e.g. https://eventspartner.vercel.app/api/integrations/meta/callback
META_REDIRECT_URI=
# Graph API version, e.g. v25.0
META_GRAPH_API_VERSION=
# Any long random string; enter the same value as the Verify Token when adding the webhook in the Meta app
META_WEBHOOK_VERIFY_TOKEN=
# 32 random bytes, base64 (openssl rand -base64 32). Encrypts Page access tokens at rest. Changing it disconnects every Page.
META_TOKEN_ENCRYPTION_KEY=
# Optional: Facebook Login for Business configuration ID (sent as config_id instead of scope)
META_LOGIN_CONFIG_ID=
